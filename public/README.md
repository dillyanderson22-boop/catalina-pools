Replace these placeholders with the client's real assets:

- logo.png — Catalina Pools wordmark (currently pulled from client CDN)
- og.jpg — 1200×630 Open Graph image
- hero.jpg — primary hero image (optional, WebGL shader is the default)
- photos/*.jpg — gallery photos from completed projects

Until those drop here, the site pulls live images from `catalinapoolsaz.com`
and Unsplash via Next.js remote patterns (already configured in next.config.js).
